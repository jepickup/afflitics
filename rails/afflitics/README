Rails
RSpec
FactoryGirl
Mongoid
Devise

Resque + resque_spec:

  To launch the workers:

  QUEUE=* rake environment resque:work

  To add a job to queue:

  /home/queue

  To start resque-web: resque-web

Resque-scheduler:

  rake resque:scheduler

  bundle exec resque-web config/initializers/resque_config.rb
